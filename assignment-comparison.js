// Online Javascript Editor for free
// Write, Edit and Run your Javascript code using JS Online Compiler

console.log("Try programiz.pro");

let a = 5;
let b = "5";

console.log(`The value in variable a is ${a} and its datatype is "${typeof a}"`)
console.log(`The value in variable b is ${b} and its datatype is "${typeof b}"`)


console.log("Let compare the values of these two variables using '==' operator")

if (a == b) {
    console.log("Both the values are equal using '=='")
} else {
    console.log("Both values are not equal using '=='")
}

console.log("Now lets compare the values of two variables using '===' operator")

if (a === b) {
    console.log("Both values are equal using '==='")
} else {
    console.log("Both values are not equal using '==='")
}

